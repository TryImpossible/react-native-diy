import 'react';
import 'react-native';

// /// 统一控制Text不根据系统"字体大小"进行缩放
// var _ = require('lodash');
// Text.render = _.wrap(Text.render, function(func, ...args) {
//   let originText = func.apply(this, args);
//   return React.cloneElement(originText, {allowFontScaling: false});
// });
